# Defaults loaded by Vite during `pnpm dev`.
# Enables the optional relay-client connection so an external relay server
# can drive this browser instance during development. Production builds
# (e.g. for GitHub Pages) leave this unset and the relay code path is
# tree-shaken out of the bundle.
VITE_RELAY_ENABLED=true
